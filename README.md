# vim-init

My vim config.

Many thanks to skywind3000 (https://github.com/skywind3000/vim-init).

Usage
```bash
mkdir -p ~/.vim/init
cd ~/.vim/init
git clone  # this repo
cd packmanager
vi packlist.txt  # add plugins
make
vi ~/.vimrc  # source ~/.vim/init/init.vim
```
